# 调参规则

## 强制前置步骤
每次给出调参建议前，必须先 Read `docs/控制链路优化方案.md` 全文。

## 调参幅度限制
- 单参数每次改动 ≤ ±20%
- 每轮只改1个参数（强制单变量原则）
- 禁止大幅跳跃（如0.14→0.08的-43%跳变）

## 参数三角关系查验
每次调参前按 MD §4.2.1 的三角关系验证：
- gyro取能压住出弯过冲的最小值
- kp1跟线主力
- kd取能压住直道微摆的最小值
- 弯中 gyro_z=200 时 gyro×200 < kp1×err，确保P不被gyro吃光
